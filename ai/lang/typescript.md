# TypeScript

## Toolchain

Node 24 from nix; global npm installs land in `~/.npm-global/bin`.

Prefer the project's own toolchain and its `package.json` scripts. Check the
lockfile for which package manager the repo uses before running an install.

`nclasp` is aliased to `npx clasp` for Google Apps Script projects.
