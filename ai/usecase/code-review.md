# Code review

Check the diff against `~/.ai/coding.md` and the matching `~/.ai/lang/` doc.

Report findings back in chat. Don't post them to the PR (`gh pr comment`,
`gh pr review`, etc.) unless the user explicitly asks for that.

